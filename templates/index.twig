<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Document</title>
    <link rel="stylesheet" href="assets/style.css">   
</head>
<body>

 <header class=header>

      <a href="{{ siteUrl }}" target=_self> <img src="assets/images/logo.png" height=80 width=80 class=imgLogo> </a> 

        <nav>
                  
            
            {% for link in header.navigation.all() %}

                    {% set destination = link.linkDestination.One() %}

                <a href="{{ destination.url }}" class=nav-link>{{ link.linkText }}</a>

                  

            {% endfor %}

                     
        </nav>

    </header>
   
    {% block content %}
        {# #}
    {% endblock %}

</body><br><br>

<footer class=footer>

<h3>Copyright</h3>

</footer>
</html>